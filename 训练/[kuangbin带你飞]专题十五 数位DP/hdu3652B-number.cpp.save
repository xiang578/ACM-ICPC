#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
ll dp[11][10][2][13];

ll get(ll n)
{
    
}
int main()
{
    int i,j,k,l;
    ll n,ans;
    memset(dp,0,sizeof(dp));
    dp[0][0][0][0]=1;
    for(i=1;i<=9;i++)
    {
        for(j=0;j<=9;j++)
        {
            
        }
    }
    while(~scanf("%lld",&n))
    {
        printf("%lld\n",get(n));
    }
    return 0;
}