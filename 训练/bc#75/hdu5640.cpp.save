#include<bits/std++.h>
using namespace std;

int main()
{
    int n,m;
    int ans,_;
    scanf("%d",&_);
    while(_--)
    {
        scanf("%d%d",&n,&m);
        ans=0;
        if(m>n) swap(n,m);
        while(1)
        {
            if()
        }
        if(n==1) ans+=m;
        else if(m==1) ans+=n;
        printf("%d\n",ans);
    }
    return 0;
}