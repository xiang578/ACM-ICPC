#include<bits/stdc++.h>
using namespace std;

int a[120];

int main()
{
    int n,m,i,f;
    while(~scanf("%d%d",&n,&m))
    {
        memset(a,0,sizeof(a));
        for(i=0;i<n;i++)
        {
            cin>>f;
            a[f]
        }
    }
    return 0;
}
