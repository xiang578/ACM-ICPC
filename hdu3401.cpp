#include <cstdio>
#include <cstring>
#include <alogrithm>
using namespace std;
const int N = 2048;
int t,p,w,as[N],bs[N],ap[N],bp[N],dp[N][N];

int main()
{
    int _;
    scanf("%d",&_);
    while(_--)
    {
        scanf("%d%d%d",&t,&p,&w);
        for(int i = 0; i < t; i++)
        {
            scanf("%d%d%d",ap+i,bp+i,as+i,bs+i);
        }


    }
    return 0;
}
